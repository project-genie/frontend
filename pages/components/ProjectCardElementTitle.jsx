import React from "react";

const ProjectCardElementTitle = ({ title }) => {
  return (
    <div>
      <h1 className="text-xs uppercase text-neutral-500 font-bold">{title}</h1>
    </div>
  );
};

export default ProjectCardElementTitle;
